# Route Store
Route store allows mapping object types to different storz

## Usage
```
store = RouterStore(
    default_store,
    {
        model.Type1: store1,
        model.Type2: store2,
    })
```
