
	def Metadata(entity):
		return entity.Meta_

	def PrimaryKey(entity):
		return str(entity.Pkey)

	def NameIdentity(pkey):
		return store.ObjectIdentity(f"{ {{ data.IdentityPrefix() }} }/{ pkey }")

	def NameKindIdentity():
		return store.ObjectIdentity(f"{ {{ data.IdentityPrefix() }} }/")

	def NameKind():
		return "{{ data.Name }}"
