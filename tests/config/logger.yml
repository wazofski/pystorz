version: 1
formatters:
  simple:
    format: '%(asctime)s | %(name)s | %(levelname)s | %(message)s'
handlers:
  console:
    class: logging.StreamHandler
    level: DEBUG
    formatter: simple
    stream: ext://sys.stdout
loggers:
  pystorz.mgen:
    level: INFO
    handlers: [console]
    propagate: no
  pystorz.sql:
    level: ERROR
    handlers: [console]
    propagate: no
  test.store:
    level: INFO
    handlers: [console]
    propagate: no
root:
  level: INFO
  handlers: [console]
